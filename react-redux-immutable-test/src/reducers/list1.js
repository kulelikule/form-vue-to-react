let initialState = [{
    name: 'name1',
    age: '1'
}, {
    name: 'name2',
    age: '2'
}]
export default (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state
    }
}